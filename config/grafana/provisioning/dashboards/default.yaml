apiVersion: 1

providers:
  - name: 'Prometheus'
    orgId: 1
    folder: 'Vote Demo'
    folderUid: ''
    type: file
    updateIntervalSeconds: 10
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards
